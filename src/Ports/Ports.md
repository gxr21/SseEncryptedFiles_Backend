# 🔌 Ports Layer

هذه الطبقة تحتوي واجهات مجردة فقط (Interfaces)
ومهمتها فصل الـ UseCases عن الـ Adapters.

========================================================

## 📌 دور الطبقة

تعريف التعاقد (Contract) بين UseCase وبين أي خدمة خارجية

تحديد شكل المدخلات والمخرجات

تخصيص واجهات Repository

تخصيص واجهات الخدمات مثل:

TokenServicePort

EmailPort

CloudStoragePort

KMSPort

CryptoEnginePort

========================================================

## ما يمنع داخل 📌 Ports

✘ لا تحتوي على أي منطق
✘ لا يوجد فيها كود حقيقي
✘ لا تحتوي على استدعاءات خارجية
✘ لا تعتمد على أي Framework
✘ لا تحتوي على أي كود قابل للاختبار

========================================================

## 📌 مبدأ مهم

Ports تمثل "ما يحتاجه النظام"
Adapters تمثل "كيف يتم تنفيذ ذلك".

========================================================

## تقسيم الملفات الخاصة بها 📌

Ports/
├── repositories/
│   ├── UserRepositoryPort.ts
│   └── FileRepositoryPort.ts
│   └── FolderRepositoryPort.ts
├── services/
│   ├── TokenServicePort.ts
│   ├── EmailPort.ts
│   ├── CloudStoragePort.ts
│   ├── KMSPort.ts
│   └── CryptoEnginePort.ts
└──  ....

========================================================

## 📌 مثال على Port

```typescript
interface UserRepositoryPort {
    createUser(user: User): Promise<User>;
    getUserById(id: string): Promise<User | null>;
    updateUser(id: string, user: Partial<User>): Promise<User>;
    deleteUser(id: string): Promise<void>;
}
