# ⚙ Application Layer

طبقة Use Cases هي اللي تحتوي منطق التطبيق (Application Logic)،
وتمثل "العمليات" التي يستطيع المستخدم القيام بها داخل النظام.

دور الطبقة

تنفيذ Use Cases مثل:

تسجيل الدخول

إنشاء حساب

رفع ملف

تنزيل ملف

حذف/نقل ملف

إنشاء مجلد

مشاركة ملف

التنسيق بين Domain وبين Ports & Adapters

==================================================================
وظائف الطبقة 📌

تستقبل البيانات من الـ Controllers

تتحقق من صحتها

تطبق قواعد الـ Domain

تتواصل مع Ports (Interfaces) لتنفيذ العمليات الخارجية

ترجع نتيجة واضحة للـ Controllers

==================================================================
ما يمنع داخل Use Cases 📌

✘ يمنع استدعاء أي Adapter
✘ يمنع استدعاء أي خدمة خارجية مباشرة
✘ يمنع استدعاء UseCase آخر
✘ يمنع التعامل مع HTTP أو Response
✘ يمنع التعامل مع قاعدة البيانات مباشرة
✘ يمنع التعامل مع الـ Controllers
✘ يمنع التعامل مع الـ Domain مباشرة
✘ يمنع التعامل مع الـ Infrastructure
==================================================================
ما يسمح به📌

استعمال Interfaces (Ports) فقط

استدعاء Domain Entities

تنفيذ Business Logic

تنفيذ خطوات سيناريو واحد

إرجاع Success/Failure بدون تفاصيل تقنية
==================================================================
📌 مبدأ مهم

UseCase يعرف Ports
لكن ما يعرف من ينفذها.
التنفيذ يتم داخل Adapters.
==================================================================
تقسيم الملفات الخاصة بها📌

\UseCases
├── UserUseCases
│   ├── RegisterUserUseCase.cs
│   ├── LoginUserUseCase.cs
│   └── UpdateUserProfileUseCase.cs
├── FileUseCases
│   ├── UploadFileUseCase.cs
│   ├── DownloadFileUseCase.cs
│   ├── DeleteFileUseCase.cs
│   └── MoveFileUseCase.cs
├── FolderUseCases
│   ├── CreateFolderUseCase.cs
│   ├── DeleteFolderUseCase.cs
│   └── MoveFolderUseCase.cs
└── SharingUseCases
    ├── ShareFileUseCase.cs
    ├── ShareFolderUseCase.cs
    └── RevokeSharingUseCase.cs

## 📌 Use Case
- هو وحدة عمل (Unit of Work) في النظام.
- يمثل "ما الذي يمكن للمستخدم فعله" في النظام.
- مثل: تسجيل مستخدم جديد، تسجيل الدخول، إضافة منتج، إلخ.
- كل Use Case هو عبارة عن "عملية" أو "عملية تجارية" (Business Process).
- كل Use Case هو عبارة عن "عملية" أو "عملية تجارية" (Business Process).
- كل Use Case هو عبارة عن "عملية" أو "عملية تجارية" (Business Process).
