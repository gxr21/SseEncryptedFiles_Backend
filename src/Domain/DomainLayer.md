# 🧩 Domain Layer

طبقة الـ Domain هي قلب النظام، وتمثل قوانين المشروع الأساسية بدون أي تفاصيل تقنية.
هذه الطبقة يجب أن تبقى أنظف طبقة بالمشروع، لأنها ما تعتمد على أي طبقة ثانية.

## 🎯 دور الطبقة

تعريف Entities (النماذج الأساسية)

تعريف القواعد (Rules) الخاصة بالكائنات

تعريف القيمة (Value Objects) إذا وجدت

تعريف المنطق العام الذي لا يحتاج أي خدمة خارجية

وضع الـ Business Constraints:

مثل: اسم الملف يجب ألا يكون فارغاً

المستخدم يجب أن يكون Verified

الملف يجب أن يكون مرتبط بمالك معيّن

المجلد لا يمكن أن يحتوي ملفات لمستخدم آخر

## ما يجب أن لا تحتوي عليه الطبقة

✘ لا تعتمد على قاعدة بيانات
✘ لا تعتمد على خدمات خارجية مثل AWS
✘ لا تحتوي على أي أكواد JWT
✘ لا يوجد فيها أي Adapter
✘ لا توجد فيها أي تفاعلات مع HTTP / Controllers
✘ لا توجد فيها أي أكواد من طبقة Infrastructure
✘ لا توجد فيها أي أكواد من طبقة Application
✘ لا توجد فيها أي أكواد من طبقة Presentation
✘ لا توجد فيها أي أكواد من طبقة UI

## 📌 ملاحظة

- يجب أن تكون هذه الطبقة مستقلة عن أي طبقة أخرى
- يجب أن تكون هذه الطبقة قابلة لإعادة الاستخدام
- يجب أن تكون هذه الطبقة قابلة للاختبار
- يجب أن تكون هذه الطبقة قابلة للتطوير
- يجب أن تكون هذه الطبقة قابلة للقراءة
- يجب أن تكون هذه الطبقة قابلة للتوثيق

## ما تحتوي عليه

كائن User

كائن File

كائن Folder

كائن Sharing

Validation Rules

Domain Logic فقط

## مبدأ مهم

Domain Layer لا يعرف بمن يستعمله.
باقي الطبقات تعتمد عليه، لكن هو لا يعتمد على أي شيء.

## تقسيم الملفات الخاصة بها

\Domain
├── Entities
│   ├── User.cs
│   ├── File.cs
│   ├── Folder.cs
│   └── Sharing.cs
├── ValidationRules
│   ├── UserValidationRules.cs
│   ├── FileValidationRules.cs
│   ├── FolderValidationRules.cs
│   └── SharingValidationRules.cs
└── DomainLogic
    ├── UserDomainLogic.cs
    ├── FileDomainLogic.cs
    ├── FolderDomainLogic.cs
    └── SharingDomainLogic.cs
